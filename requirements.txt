# Jupiter Shot — Month 1 Dependencies
# Pinned for CUDA 12.1, Python 3.10, 8× A100 80GB
# Last validated: 2025-08-02

# ── Core ML ──────────────────────────────────────────────────────────────────
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
# Install with: pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cu121

# ── Distributed Training ─────────────────────────────────────────────────────
deepspeed==0.14.2
# NOTE: Megatron-Core requires a separate install from source (see RUNBOOK.md)
# megatron-core is NOT on PyPI; clone from https://github.com/NVIDIA/Megatron-LM

# ── Hugging Face Ecosystem ───────────────────────────────────────────────────
transformers==4.40.2
datasets==2.19.1
tokenizers==0.19.1
huggingface-hub==0.23.2
accelerate==0.30.1
sentencepiece==0.2.0
safetensors==0.4.3

# ── Quantization ─────────────────────────────────────────────────────────────
auto-gptq==0.7.1
autoawq==0.2.5
# NOTE: llama-cpp-python requires separate build with CUDA support:
# CMAKE_ARGS="-DLLAMA_CUBLAS=on" pip install llama-cpp-python==0.2.77

# ── Inference ────────────────────────────────────────────────────────────────
vllm==0.4.2
# NOTE: text-generation-inference (TGI) is a Rust binary; install via Docker:
# docker pull ghcr.io/huggingface/text-generation-inference:2.1.0

# ── Evaluation ───────────────────────────────────────────────────────────────
lm-eval==0.4.2
# NOTE: lm-evaluation-harness package name is lm-eval on PyPI

# ── Logging & Monitoring ─────────────────────────────────────────────────────
tensorboard==2.16.2
wandb==0.17.0  # optional; set WANDB_DISABLED=true to skip

# ── Data Processing ──────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
pyarrow==16.0.0
xxhash==3.4.1
zstandard==0.22.0
tqdm==4.66.4
pyyaml==6.0.1
omegaconf==2.3.0

# ── Utilities ────────────────────────────────────────────────────────────────
click==8.1.7
rich==13.7.1
psutil==5.9.8
pynvml==11.5.0
requests==2.32.2
fastapi==0.111.0
uvicorn==0.29.0
httpx==0.27.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-cov==5.0.0
pytest-asyncio==0.23.7

# ── Type Checking ────────────────────────────────────────────────────────────
mypy==1.10.0
