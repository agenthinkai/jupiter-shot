# Jupiter Seed 4B — Pinned CPU-only dependencies
# Install with: .venv/bin/pip install -r requirements/seed_4b.txt
# Do NOT install transformers/torch/peft here for CPU-only test runs;
# those are only needed for GPU training (see requirements/seed_4b_gpu.txt).

# Test framework
# pytest already provides --junitxml natively; no plugin needed
pytest==8.3.5

# Inference server (inference_server.py)
fastapi==0.115.12
httpx==0.28.1
starlette==0.46.2
uvicorn==0.34.2

# Utilities
pyyaml==6.0.2
tqdm==4.67.1
